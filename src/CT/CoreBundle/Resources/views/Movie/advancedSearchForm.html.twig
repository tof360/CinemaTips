<div class="well container">
    <h4>Formulaire de recherche</h4>

    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}




    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.originality, "Originatlité", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.originality) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.violence, "Taux de violence", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.violence) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.twist, "Retournement final/Twist", {'label_attr': {'class': 'col-sm-3 control-label pull-middle'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.twist) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.complexity, "Complexité", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.complexity) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.emotion, "Emotions", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.emotion) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.specialEffects, "Effets spéciaux", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
        <div class="col-sm-9">
            {# Génération de l'input. #}
            {{ form_widget(form.specialEffects) }}
        </div>
    </div>

    {{ form_widget(form.save, {'label': 'C\'est noté !'} , {'attr': {'class': 'btn btn-success'}}) }}
    {# Génération automatique des champs pas encore écrits.
       Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
       et tous les champs cachés (type « hidden »). #}
    {{ form_rest(form) }}


    {# Fermeture de la balise <form> du formulaire HTML #}
    {{ form_end(form) }}
</div>